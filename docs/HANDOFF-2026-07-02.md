# 任务交接 · 2026-07-02

> 本轮工作：全仓现状梳理 → 四大问题诊断 → 五阶段代码落地。全部改动已通过回归，未提交 git。

## 一、产出

### 文档（供与 Claude 讨论结构性重构）

| 文档 | 内容 |
|---|---|
| [鹰眼-现状梳理与重构议题-briefing.md](鹰眼-现状梳理与重构议题-briefing.md) | 全仓四维现状 + 七类落差账本（带证据）+ 六大重构议题 + 三大战略分叉 |
| [鹰眼-四大问题诊断与改进建议.md](鹰眼-四大问题诊断与改进建议.md) | KB录入效率 / 医院接入+OCR / 自查自纠 / 规则粒度 四问题的代码级诊断、建议与**落地进度记录** |

### 代码（五阶段，15 项全部完成）

- **A 修复**：verified 判断 bug（爬虫条目不再冒充已核验）；violation_nature 接入单案稽核；机构画像/三阶段支持 exam 子集（`?mode=exam`）；规则数口径统一 58 + build 校验。
- **B KB 提效**：两库 parser 表头加固 + 双层质量门；**存量坏行已清洗 1467→1295 条**；新增 `scripts/bulk-import-local.mjs` 本地批量入库（`--dry-run`/`--max-pdf-mb`）；demoOnly 用 `KB_FULL_IMPORT=1` 可解除。
- **C Intake**：结算清单/DRG/追溯码/运行指标四槽位接入 merge；费用行支持 `item_code`/`insurance_code`。
- **D 自查闭环**：236 条官方清单可勾选工作台（`/api/checklist/full` + `/api/checklist/progress`，priority.html 弹窗）；自查复跑快照+diff（`/api/exam/diff`，工作台横幅）；主动退回测算（`/api/exam/refund-estimate`，自动附在自查清单导出末尾）。
- **E 规则机制**：`applyRelationsArbitration`（audit-engine.js，reconcile 前）——subsumes / 互斥备择定性 / 全局豁免四场景。

## 二、验证状态

- `bash yhf/run.sh --strict` → **overall PASS**（G0 干净件零误报保持）
- `node scripts/verify-dashboard-frontend.js` → PASS
- `node scripts/run-priority-gold-eval.js` → 7/7
- 端到端 curl 冒烟（exam 审核/diff/测算/清单/画像）全部通过；无 linter 错误
- 主案卷结果不变（疑点 5 / 线索 2）；测试产生的运行态文件已清理

## 三、遗留（刻意不做，等战略定）

1. **规则拆分**（A-107/A-110 原子化、重复收费族合并、T 系 extends 化）——需同步改金标准/expected_findings/gold。
2. **真实 HIS 拉取器** + API/OCR 按就诊号合并 + 异步批量队列。
3. **KB 待核实队列 UI**、col109 定时监控、全量目录抓取（开关已备好）。
4. v7 prompt 全量回归、Claude 目标模型验证、eval↔engine 端到端打通（见 briefing 议题 6）。

## 四、快速上手

```bash
cd prototype/app && node server.js          # http://localhost:3700
bash yhf/run.sh --strict                     # 改审核行为后必跑
node scripts/bulk-import-local.mjs <目录> --dry-run   # KB 批量导入预览
node scripts/clean-kb-badrows.mjs --dry-run  # 坏行清洗预览
```

下一步建议：带两份文档去和 Claude 聊三大分叉（广覆盖 vs 单场景 / 确定性 vs 真LLM / demo 数据 vs 真实数据），聊定后再动"遗留"清单；改动确认没问题后 git 提交。
